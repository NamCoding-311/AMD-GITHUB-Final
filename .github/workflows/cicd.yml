name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      # DEBUG: Kiểm tra toàn bộ cấu trúc repo
      - name: Debug Folder Structure
        run: |
          echo "=== ROOT FOLDER ==="
          ls -R .
          echo "=== AMD_final FOLDER ==="
          ls -R AMD_final || true
          echo "=== urlshorten FOLDER ==="
          ls -R AMD_final/urlshorten || true
          echo "=== BE FOLDER ==="
          ls -R AMD_final/urlshorten/BE || true

      # BUILD BACKEND
      - name: Build Backend
        run: dotnet build AMD_final/urlshorten/BE/URLShortener.API.csproj --configuration Release

      # RUN TESTS
      - name: Run Tests
        run: dotnet test AMD_final/urlshorten/BE/URLShortener.API.csproj --no-build

      # BUILD DOCKER IMAGE
      - name: Build Docker Image
        run: |
          echo "Building Docker image..."
          echo "Username = ${{ secrets.DOCKER_USER }}"
          echo "=== LIST BE FOLDER BEFORE BUILD ==="
          ls -R AMD_final/urlshorten/BE
          docker build -t ${{ secrets.DOCKER_USER }}/urlshortener:${{ github.sha }} AMD_final/urlshorten/BE

      # LOGIN DOCKER HUB
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      # PUSH IMAGE
      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USER }}/urlshortener:${{ github.sha }}

      # DEPLOY RENDER
      - name: Deploy to Render
        run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
